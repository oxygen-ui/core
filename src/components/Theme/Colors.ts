export default interface Colors {
    primary: string;
    secondary: string;
    error: string;
    info: string;
    success: string;
    warning: string;
}

export type Color = 'primary' | 'secondary' | 'error' | 'info' | 'success' | 'warning';
